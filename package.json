{
  "scripts": {
    "start:server": "cd server && npm run start:dev",
    "start:app": "cd app && npm run start:dev",
    "test:playwright": "npx playwright test",
    "start:all": "npm-run-all --parallel start:server start:app"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "npm-run-all": "^4.1.5",
    "playwright": "^1.52.0"
  },
  "dependencies": {
    "md5": "^2.3.0"
  }
}
